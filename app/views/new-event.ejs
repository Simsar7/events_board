<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= typeof event !== 'undefined' && event ? "Edit Event" : "Create Event" %> - Events Board</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css">
</head>
<body class="bg-gray-100 min-h-screen">

<!-- Navigation Header -->
<nav class="bg-white shadow-sm mb-8">
  <div class="max-w-6xl mx-auto px-8 py-4 flex justify-between items-center">
    <a href="/" class="text-xl font-bold text-blue-600">Events Board</a>
    <div class="flex gap-6 items-center">
      <a href="/" class="text-gray-600 hover:text-gray-900">Home</a>
      <a href="/admin/events" class="text-gray-600 hover:text-gray-900">Manage Events</a>
      <% if (typeof user !== 'undefined' && user) { %>
        <span class="text-gray-600">Hi, <%= user.username %></span>
        <a href="/logout" class="text-red-600 hover:text-red-800">Logout</a>
      <% } %>
    </div>
  </div>
</nav>

<!-- Main Content -->
<div class="max-w-2xl mx-auto px-8">
  <div class="mb-6">
    <a href="/admin/events" class="text-blue-600 hover:underline">← Back to Events</a>
  </div>

  <h1 class="text-3xl font-bold mb-6">
    <%= typeof event !== 'undefined' && event ? "Edit Event" : "Create New Event" %>
  </h1>

  <form method="POST"
        action="<%= typeof event !== 'undefined' && event ? '/admin/events/' + event.id : '/admin/events' %>"
        class="bg-white p-8 rounded-lg shadow-md space-y-5">

    <div>
      <label class="block text-sm font-medium text-gray-700 mb-2">Title *</label>
      <input name="title" required
        value="<%= typeof event !== 'undefined' && event && event.title ? event.title : '' %>"
        placeholder="Event Title"
        class="w-full border border-gray-300 p-3 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent" />
    </div>

    <div>
      <label class="block text-sm font-medium text-gray-700 mb-2">Description</label>
      <textarea name="description" rows="5"
        placeholder="Event Description"
        class="w-full border border-gray-300 p-3 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent"><%= typeof event !== 'undefined' && event && event.description ? event.description : '' %></textarea>
    </div>

    <div>
      <label class="block text-sm font-medium text-gray-700 mb-2">Date(s) *</label>
      <input name="dates" id="datePicker" required
        value="<%= typeof event !== 'undefined' && event && event.dates ? event.dates : '' %>"
        placeholder="Click to select date(s)"
        class="w-full border border-gray-300 p-3 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent cursor-pointer" />
      <p class="text-sm text-gray-500 mt-1">💡 Click to select a single date or drag to select a range</p>
    </div>

    <div>
      <label class="block text-sm font-medium text-gray-700 mb-2">Location</label>
      <input name="location"
        value="<%= typeof event !== 'undefined' && event && event.location ? event.location : '' %>"
        placeholder="Event Location"
        class="w-full border border-gray-300 p-3 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent" />
    </div>

    <div>
      <label class="block text-sm font-medium text-gray-700 mb-2">Image URL</label>
      <input name="image_url" type="url"
        value="<%= typeof event !== 'undefined' && event && event.image_url ? event.image_url : '' %>"
        placeholder="https://example.com/image.jpg"
        class="w-full border border-gray-300 p-3 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent" />
      <p class="text-sm text-gray-500 mt-1">Optional: Add a banner image for your event</p>
    </div>

    <div class="flex items-start">
      <div class="flex items-center h-5">
        <input type="checkbox" name="featured" value="true" id="featured"
          <%= typeof event !== 'undefined' && event && event.featured ? "checked" : "" %> 
          class="w-5 h-5 text-blue-600 border-gray-300 rounded focus:ring-blue-500" />
      </div>
      <div class="ml-3">
        <label for="featured" class="font-medium text-gray-700">Featured Event</label>
        <p class="text-sm text-gray-500">Display this event prominently on the homepage</p>
      </div>
    </div>

    <div class="flex gap-3 pt-6 border-t border-gray-200">
      <button type="submit" class="bg-blue-600 text-white px-6 py-3 rounded-lg hover:bg-blue-700 font-medium transition-colors">
        <%= typeof event !== 'undefined' && event ? "Update Event" : "Create Event" %>
      </button>
      <a href="/admin/events" class="bg-gray-200 text-gray-700 px-6 py-3 rounded-lg hover:bg-gray-300 inline-flex items-center font-medium transition-colors">
        Cancel
      </a>
    </div>

  </form>
</div>

<!-- Flatpickr JS -->
<script src="https://cdn.jsdelivr.net/npm/flatpickr"></script>
<script>
  flatpickr("#datePicker", {
    mode: "range",           // Allow selecting date ranges
    dateFormat: "Y-m-d",     // Format: 2025-01-27
    altInput: true,          // Show user-friendly format
    altFormat: "F j, Y",     // Display as: January 27, 2025
    allowInput: true,        // Allow manual input
    minDate: "today"         // Don't allow past dates
  });
</script>

</body>
</html>